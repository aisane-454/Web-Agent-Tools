核心判断：这个工具只在"外层模型缺上下文"或"判断错误代价高"两类场景有不可替代价值，接口设计的关键是让外层"只买结论、不买过程"，内部工具循环可以缓行。

问题1：有不可替代价值的任务形态
可操作的判断特征只有三条：① 利用被咨询方独有上下文——联网检索、新版本 SDK/依赖文档、小众库的 issue 讨论，这些是外层模型静态权重里没有的信息；② 高代价决策的第二意见——架构选型、疑难 bug 归因、安全漏洞判断，此时多模型分歧本身就是信号，共识提高置信度
medrxiv.org
；③ 长文压短——粘贴大段日志/文档让网页模型提炼结构化要点，外层只收结论。除此之外的"多一个视角"确实净收益为负，不必挽留。

问题2：接口设计

问题3：纯咨询 vs 内部循环
先把"咨询+续问+压缩"做深，再考虑循环。若做后者，安全边界原则是：被咨询方是不受信任的顾问，不是执行者
workos.com
。具体：只读工具白名单（web search、fetch URL、read file），禁止一切写操作（shell、文件写入、对外发送）；每次委托步数与 token 预算硬上限；全部工具调用轨迹随结论回传，供外层审计
augmentcode.com
。